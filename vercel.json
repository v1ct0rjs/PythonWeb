{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "public"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "buildCommand": "python3 -m pip install -r requirements.txt && reflex init && REFLEX_ENV=prod API_URL=https://pythonweb-backend-production.up.railway.app reflex export --frontend-only && unzip frontend.zip -d public"
}
